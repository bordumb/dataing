# Frontend Environment Variables
# Copy to .env.local for local development

# API URL - leave empty for local dev (uses Vite proxy)
# Set to your backend URL for production (e.g., https://api.yourdomain.com)
VITE_API_URL=
