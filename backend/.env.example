# Anthropic API Key (required for investigations)
ANTHROPIC_API_KEY=sk-...

# LLM Model (optional, defaults to claude-sonnet-4-20250514)
# LLM_MODEL=claude-sonnet-4-20250514

# Database URLs (set automatically by `just demo`)
# DATABASE_URL=postgresql://datadr:datadr@localhost:5432/datadr_demo
# APP_DATABASE_URL=postgresql://datadr:datadr@localhost:5432/datadr_demo

# SMTP Configuration (required for email-based password reset)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM_EMAIL=noreply@dataing.io
# SMTP_FROM_NAME=Dataing
# SMTP_USE_TLS=true

# Password Recovery Configuration
# PASSWORD_RECOVERY_TYPE controls how password resets are delivered:
#   auto (default) - Uses email if SMTP configured, otherwise console
#   email - Requires SMTP to be configured
#   console - Prints reset link to server console (demo/dev mode)
#   admin_contact - Shows "contact your admin" message (for SSO orgs)
# PASSWORD_RECOVERY_TYPE=auto
# ADMIN_EMAIL=admin@example.com

# Frontend URL (used for building links in emails)
FRONTEND_URL=http://localhost:3000
